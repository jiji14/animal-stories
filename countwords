# count words

def countWords(text):
    word_list = text.split()
    dic = {}
    for word in word_list:
        dic[word] = dic.get(word, 0) + 1
    return dic
  
dic = countWords("i like carrots like rabbit")
print(countWords("i like carrots like rabbit"))
